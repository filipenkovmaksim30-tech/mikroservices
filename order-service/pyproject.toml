[project]
name = "order-service"
version = "0.1.0"
description = "Production messaging practice with RabbitMQ and Kafka"
requires-python = ">=3.13,<3.15"
dependencies = [
    "aio-pika>=9,<10",
    "aiokafka>=0.12,<1",
    "aiosmtplib>=4,<5",
    "alembic>=1.16,<2",
    "asyncpg>=0.30,<1",
    "fastapi>=0.116,<1",
    "pydantic[email]>=2.12,<3",
    "pydantic-settings>=2.10,<3",
    "sqlalchemy[asyncio]>=2,<3",
    "structlog>=25.4,<26",
    "uvicorn[standard]>=0.35,<1",
    "pyjwt[crypto]>=2.13.0",
    "httpx>=0.28.1",
]

[dependency-groups]
dev = [
    "mypy>=1.18,<2",
    "pytest>=8.4,<9",
    "pytest-asyncio>=1.2,<2",
    "ruff>=0.14,<1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["messaging_lab"]

[tool.pytest.ini_options]
addopts = "-ra --strict-config --strict-markers"
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py313"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "ASYNC"]

[tool.mypy]
python_version = "3.13"
strict = true
packages = ["messaging_lab"]
